plugins {
    id("io.github.balticamadeus.pope")
}

pope {{{PROJECT_ROOT_BLOCK}}
}

// Registries live in pope-registries.properties, at the ABL project root
// (see projectRoot above, if set) - not here. See pope's README.md.
// Run `pope registry add <prefix> <url>` to add one.
